import React from 'react'

const MyOrder = () => {
    const showTime = (da) => {
        const d = new Date(da);
        return `${d.getHours()}:${d.getMinutes()}`
    }
  return (
    <div>
      
    </div>
  )
}

export default MyOrder
